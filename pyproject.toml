[project]
name = "portfolio-management-in-quant-finance"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.4",
    "pytest>=9.0.3",
    "pyyaml>=6.0.3",
]
